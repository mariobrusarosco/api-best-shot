name: Deploy Demo Database Migrations

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  apply_migrations:
    runs-on: ubuntu-latest
    environment: demo

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: yarn install

      - name: Apply migrations
        run: |
          # Hard-coded solution for now - with URL-encoded special characters
          # We'll move this to a secret once it's confirmed working
          # DB_STRING_CONNECTION: ${{ secrets.DB_STRING_CONNECTION }}

          # Execute using environment variable directly
          DB_STRING_CONNECTION="postgresql://postgres.pppzfkndwrtekdyfknym:W%23TgSCcVER4m%257@aws-0-us-east-1.pooler.supabase.com:6543/postgres" npx drizzle-kit migrate
